Backend development uses `npm run dev` (tsx watch) and database migrations via `npm run prisma:migrate` or `npm run prisma:push`; the admin seed account is created with `npm run seed` (`src/scripts/seedAdmin.ts`). The build pipeline runs `prisma generate && tsc` before starting the Node server. Environment variables are loaded from `.env` (JWT_SECRET, database URL, Google AI key).