Claim submission requires at least one attached image before the status can be transitioned to SUBMITTED; after submission, `analyzeDamage` runs asynchronously in the background so clients poll or rely on later endpoints. Damage analysis reads files from the relative `uploads/` directory, so the server must be started from the repository root so `path.resolve('.', ...)` resolves correctly.