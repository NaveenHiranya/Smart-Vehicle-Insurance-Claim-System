# Backend API URL — leave empty in development (Vite proxy handles it)
# In production set this to your deployed backend origin (no trailing slash)
# e.g. https://autoshield-backend.railway.app
VITE_API_URL=
